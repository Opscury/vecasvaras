node_modules
dist
.DS_Store
*.local
.vite

# Vite writes these while resolving the config on Windows
vite.config.ts.timestamp-*

# Screenshot drop from the Claude desktop app
Claude outputs/

# Freesound API credential — never commit
.freesound-key


# scratch archive used to mirror the repo into a screenshot sandbox
_vv_snapshot.zip

# Screenshot harness output (the routes in tools/shots/ are kept)
/shots/

# Python bytecode from the audio tools
__pycache__/
